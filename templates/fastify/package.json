{
  "name": "template-fastify",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"node scripts/dev.js\" \"nodemon --watch dist dist/index.js\"",
    "build": "node scripts/build.js",
    "serve": "node dist/index.js"
  },
  "devDependencies": {
    "concurrently": "^7.3.0",
    "dotenv": "^16.0.2",
    "esbuild": "^0.15.7",
    "fs-extra": "^10.1.0",
    "nodemon": "^2.0.19",
    "typescript": "^4.5.3"
  },
  "dependencies": {
    "fastify": "^4.5.3"
  }
}
