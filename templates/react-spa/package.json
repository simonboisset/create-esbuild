{
  "name": "template-react-spa",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "esbuild-run dev",
    "build": "esbuild-run build"
  },
  "devDependencies": {
    "esbuild-run": "*",
    "typescript": "^4.5.3"
  }
}
